# MRA Dependencies
# Update 01-01-2026 (tried and confirmed)
# ============================================================================
# INSTALLATION INSTRUCTIONS for RTX5090 GPU (WSL + Virtual Environment)
# ============================================================================
# This installation script is built for python 3.10.10
# Use virtual environments to avoid conflicts with other projects.
#
# 1. Create virtual environment:
#    python -m venv .venv
#
# 2. Activate virtual environment:
#    source ./.venv/bin/activate
#
# 3. Update pip:
#    pip install -U pip
#
# 4. Install system dependencies (OCR):
#    sudo apt install -y tesseract-ocr pngquant ghostscript cmake g++ libjpeg-dev libpng-dev libtiff-dev zlib1g-dev
#
# 5. Install PyTorch 2.6+ FIRST (required for RTX 5090 sm_120 support):
#    pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu128
#
# 6. Install remaining Python packages:
#    pip install -r requirements_01-01-2026forRTX5090.txt
# ============================================================================


# Embedding and FAISS
numpy==2.2.6

faiss-gpu-cu12==1.13.1

# Core
python-dotenv==1.0.0
requests==2.32.5

# Updated for PyTorch 2.6+ compatibility
transformers>=4.46.0
sentence-transformers>=3.3.1
huggingface-hub>=0.26.0
tqdm==4.67.1

# Web framework
fastapi==0.122.0
uvicorn==0.38.0
pydantic==2.12.5
python-multipart==0.0.20
jinja2==3.1.6

# Web scraping
beautifulsoup4==4.14.3
lxml==6.0.2
trafilatura==1.6.3

# Web search
ddgs==9.9.2  # DuckDuckGo fallback

# Document processing
PyMuPDF==1.26.6
pymupdf4llm==0.2.2
pymupdf-layout==1.26.6
python-docx==1.1.0

# OCR (optional, WSL only)
ocrmypdf==16.12.0

# Chunking
tiktoken==0.12.0

# ollama
ollama==0.6.1

# Development
pytest==7.4.3
